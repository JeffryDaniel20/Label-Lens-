"""The golden-dataset eval harness (P7-T3, IMPLEMENTATION.md section 22).

See `evals.dataset` for the dataset format and its honestly-stated scope,
`evals.metrics` for the pure metric math, `evals.runner` for what actually
drives each case through the real pipeline, and `evals.report` for the HTML
report `evals.cli`/`make eval` produce.
"""

from __future__ import annotations
