# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Regenerated by `npm run generate:api-types` - src/api/schema.ts (checked
# in, since it's what the app actually imports) is the source of truth for
# review, not this intermediate dump.
openapi.json

# Playwright's own run artifacts.
test-results
playwright-report
blob-report
playwright/.cache

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
